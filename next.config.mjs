/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/GetEvinced",
    assetPrefix:"/GetEvinced/mcag",
  output: "export",
  reactStrictMode: true,
};

export default nextConfig;
